# 核心算法

## 数据流

```
Binance Futures API (fapi)
   │
   ├─ /fapi/v1/ticker/24hr  ─── 启动 + 每30分钟自动刷新涨幅榜 Top50
   │                            （与 MANUAL_SYMBOLS 合并，自动增删币种）
   │
   └─ /fapi/v1/klines       ─── 每 60s 轮询 15m K线
          │
          └─ 滑动窗口 (20根) ─── 每币种保留最近 K 线历史
```

## 策略架构

策略放在 `strategies/` 文件夹下，每个策略一个文件，继承 `BaseStrategy`。

```
strategies/
├── __init__.py    # 注册所有策略
├── base.py        # BaseStrategy 基类
├── surge.py       # 📈 放量拉升 + 📉 放量下跌
└── ...            # 后续策略加在这里
```

新增策略只需三步：
1. 在 `strategies/` 下新建文件，继承 `BaseStrategy`
2. 实现 `init_state()` 和 `check()` 方法
3. 在 `__init__.py` 的 `STRATEGIES` 列表里注册

### 状态结构

每个币种维护：

```
{
  "klines": [...],            # 最近 N 根 K 线（滑动窗口）
  "surge": {                  # surge 策略的独立状态
    "last_rise_alert": 0,
    "last_drop_alert": 0,
  },
  # 新增策略的 state 会追加在这里
}
```

## K线滑动窗口

- 启动时拉取 20 根 K 线初始化
- 每次轮询取最新 1 根，与窗口最后一条对比 `open_time`：
  - 相同 → 更新（K线未闭合，价格还在变）
  - 不同 → 追加新K线，超出长度则移除最老的

## Surge 策略

上涨和下跌各有一套独立的组合条件，独立冷却。

---

### 📈 放量拉升

#### 条件 1：单K线拉升

```
当前涨幅 = (close - low) / low  ≥  THRESHOLD（2%）
```

同一根 K 线内从低点拉到高点，说明有买方力量介入。

#### 条件 2：成交量放大

```
均值 = 过去 VOLUME_WINDOW（10）根已闭合 K 线成交量均值
倍数 = 当前K线成交量 / 均值  ≥  VOLUME_MULTIPLIER（1.5x）
```

拉升必须带量才有持续性。均值只统计已闭合 K 线（`close ≠ open`）。

#### 条件 3：前一根阳线

```
已收盘K线 满足 close > open
```

确保不是单根毛刺，有持续买入意愿。

#### 卡片样式

橙色模板 🟠

---

### 📉 放量下跌

#### 条件 1：单K线下跌

```
当前跌幅 = (high - close) / high  ≥  THRESHOLD（2%）
```

同一根 K 线内从高点跌下来，说明有卖方力量抛压。

#### 条件 2：成交量放大

同上 — 下跌放量 = 恐慌抛售。

#### 条件 3：前一根阴线

```
已收盘K线 满足 close < open
```

持续卖出，不是单根毛刺。

#### 卡片样式

紫色模板 🟣

---

### 触发流程

```
上涨检测: 拉升 ≥ 2%  +  放量 ≥ 1.5x  +  前一根阳线  →  🎯 橙色卡片
下跌检测: 下跌 ≥ 2%  +  放量 ≥ 1.5x  +  前一根阴线  →  🎯 紫色卡片
```

两个方向独立判断，互不干扰，同一次轮询可能同时触发。

## 冷却机制

上涨和下跌各自独立冷却。同一方向告警后 `ALERT_COOLDOWN（300s）`内不再重复发送，避免刷屏。
