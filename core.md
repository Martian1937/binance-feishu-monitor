# 核心算法

## 数据流

```
Binance Futures API (fapi)
   │
   ├─ /fapi/v1/ticker/24hr  ─── 启动时获取涨幅榜 Top50，合并配置币种
   │
   └─ /fapi/v1/klines       ─── 每 30s 轮询 1m K线
          │
          └─ 滑动窗口 (15根) ─── 每币种保留最近 K 线历史
```

## 状态结构

每个币种维护：

```
{
  "klines": [...],           # 最近 N 根 K 线（滑动窗口）
  "last_rise_alert": 0,      # 单K线拉升告警时间戳
  "last_trend_alert": 0,     # 趋势告警时间戳
  "last_volume_alert": 0,    # 成交量告警时间戳
}
```

## K线滑动窗口

- 启动时拉取 15 根 K 线初始化
- 每次轮询取最新 1 根，与窗口最后一条对比 `open_time`：
  - 相同 → 更新（K线未闭合，价格还在变）
  - 不同 → 追加新K线，超出长度则移除最老的
- 始终保留足够的历史数据供趋势和成交量计算

## 三维告警

### 1. 单K线拉升 🟠

```
当前涨幅 = (close - low) / low
```

当 **当前涨幅 ≥ THRESHOLD（1%）** 且距上次同类告警超过冷却期（120s），触发。

目的：捕捉**瞬间脉冲**，同一根 K 线内从低点拉到高点。

### 2. 多K线趋势 🔵

```
累计涨幅 = (最新close - N根前open) / N根前open
```

取最近 `TREND_KLINES（5根）`的 K 线，计算累计涨幅。当 **累计涨幅 ≥ TREND_THRESHOLD（2%）** 触发。

目的：过滤单根K线毛刺，识别**持续上涨**趋势。

### 3. 成交量放大 🟣

```
均值 = 过去 VOLUME_WINDOW（10）根已闭合 K 线成交量的均值
倍数 = 当前K线成交量 / 均值
```

当 **倍数 ≥ VOLUME_MULTIPLIER（2.0x）** 触发。

注意：均值只统计已闭合的 K 线（`close ≠ open`），避免未闭合 K 线数据不准。

目的：量在价先，**成交量异常放大**往往是启动的前兆。

## 冷却机制

每个维度的告警独立计时。同一种告警在 `ALERT_COOLDOWN（120s）`内不重复发送，避免刷屏。
